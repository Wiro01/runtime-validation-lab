name: WRO Monitor Android APK
on:
  push:
    branches: [wro-monitor-android-v1]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Fetch source without marketplace actions
        run: |
          git clone --depth 1 --branch wro-monitor-android-v1 https://github.com/Wiro01/runtime-validation-lab.git src
      - name: Prepare Java Android and Gradle
        run: |
          java -version
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" 'platforms;android-35' 'build-tools;35.0.0'
          curl -fsSL https://services.gradle.org/distributions/gradle-8.9-bin.zip -o /tmp/gradle.zip
          unzip -q /tmp/gradle.zip -d /tmp
      - name: Build APK
        run: |
          /tmp/gradle-8.9/bin/gradle -p src/wro-monitor-android :app:assembleDebug --no-daemon
          cp src/wro-monitor-android/app/build/outputs/apk/debug/app-debug.apk WRO-Monitor-Android.apk
          sha256sum WRO-Monitor-Android.apk
      - name: Publish latest release asset
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete wro-monitor-chat-latest --repo Wiro01/runtime-validation-lab --cleanup-tag --yes || true
          gh release create wro-monitor-chat-latest WRO-Monitor-Android.apk --repo Wiro01/runtime-validation-lab --target wro-monitor-android-v1 --title 'WRO Monitor Android Latest' --notes 'Latest WRO Monitor Android APK for direct installation.'
      - name: Publish transport branch
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          base64 -w0 WRO-Monitor-Android.apk > WRO-Monitor-Android.apk.b64
          mkdir /tmp/apk-transport
          cp WRO-Monitor-Android.apk.b64 /tmp/apk-transport/
          cd /tmp/apk-transport
          split -b 3500 -d -a 2 WRO-Monitor-Android.apk.b64 WRO-Monitor-Android.apk.b64.part-
          git init
          git checkout -b wro-monitor-apk-artifact
          git config user.name github-actions
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .
          git commit -m 'Publish WRO Monitor APK transport'
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/Wiro01/runtime-validation-lab.git"
          git push --force origin wro-monitor-apk-artifact
